node_modules/
backend/.env
backend/data/*.db
backend/data/*.db-shm
backend/data/*.db-wal
backend/.tmp/
backend/public/uploads/

# Claude Code local state — contains its own nested .git repo, which git
# can't add as a plain directory (it'd be treated as a broken submodule link).
.claude/
